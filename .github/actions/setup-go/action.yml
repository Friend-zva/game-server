name: Setup Go
description: "Sets up Go environment"

runs:
  using: composite
  steps:
    - name: Setup Go
      uses: actions/setup-go@v6
      with:
        go-version: "1.26"
        cache: true

    - name: Install dependencies
      run: go mod tidy
      shell: bash
